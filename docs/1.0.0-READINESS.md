# Is the surface actually working? A feature-by-feature audit

> ## RE-RUN 2026-08-23 -- the audit below is stale AND its method had a gap
>
> Everything under the original heading was measured on **2026-08-14 against
> library `1.0.0b10` / integration `2.14.0`**. Current is **`1.0.0b23` /
> `2.17.3`**, thirteen library releases later, and the fleet table below is
> already wrong: it records `444a7bf4` M3 Pro_180 as *offline at the cloud since
> 2026-08-11*, and that camera answered local control on 2026-08-23.
>
> ### The method gap
>
> The audit recorded that *"of fourteen attributes probed, eight acked and kept
> their own value"* and separately that one **entity** was a switch that did
> nothing. It never joined those two facts: **which entities ship on which
> models, and does each take effect on that model.** A per-attribute finding is
> not a per-entity finding, and the shipping decision is per entity.
>
> That gap hid a second dead control for nine days. On 2026-08-23 `trackingMode`
> was measured over the local channel on all three A000088 units: acknowledged
> with a `setDevAttrResp`, read-back `0` every time. Those cameras have no motor.
> An auto-tracking switch had been shipping on every camera regardless.
> Fixed in integration **2.17.3**; the same gate `button.py` had used all along.
>
> ### What the missing step shows (measured 2026-08-23 from the HA registries)
>
> Control entities per camera, by model family:
>
> | control | A000088 (x4) | A001064 (x1) | A001513 (x3) |
> |---|---|---|---|
> | status_led, motion_detection, motion_sensitivity, night_vision, microphone, speaker_volume, hdr, timestamp_overlay, voice_prompts, floodlight, floodlight_automation, siren, ir_light, auto_tracking | ships | ships | ships |
> | camera_audio | - | ships | ships |
> | ptz_left / ptz_right / ptz_stop | - | ships | - |
>
> **Only three controls are capability-gated at all.** `camera_audio` is gated on
> SDES and the PTZ buttons on the advertised direction codes -- both correct.
> Every other control ships identically on all eight cameras across three model
> families, which is why a dead one can hide: nothing in the shipping decision
> ever asks whether the model supports it.
>
> ### Effect evidence, and where it is still missing
>
> Definitive for **A000088** as of 2026-08-23 -- local control channel, owner
> account, all fourteen controls read -> set -> read back -> restore on all three
> reachable units (every write restored):
>
> - **Class A (effect confirmed by read-back):** status_led, motion_detection,
>   motion_sensitivity, night_vision_mode, microphone, speaker_volume,
>   osd_timestamp, auto_light, voice_prompts, hdr.
> - **Class E (acked and ignored):** `nightVisionIRLight` (already withdrawn in
>   2.15.0) and `trackingMode` (withdrawn for fixed cameras in 2.17.3).
> - **Not written, deliberately:** siren and floodlight. Firing an alarm or
>   lighting a bedroom is not a test. **Class B on A000088.**
>
> **A001064 and A001513 have no local control surface at all** -- neither answers
> unicast discovery, confirmed by a full /24 sweep -- so none of the above
> transfers to them. Their control cells remain **class B**: the write is
> acknowledged over the cloud and no independent read-back has confirmed effect.
> Closing them needs a cloud set plus a forced re-read per control per family,
> and on the battery models each costs a wake.
>
> ### Local and cloud views diverge
>
> Measured the same day, same attribute (`voiceEnable`) on both paths:
> Bedroom M3 Pro reports `1` locally while Home Assistant reports `off`, and has
> since before the testing. HA never moved within ~3.5 s of any local write. So
> a read-back taken from HA is not evidence about the device -- which is exactly
> the mistake this audit exists to prevent.
>
> ### Fleet, re-measured 2026-08-23
>
> | camera | model | state today |
> |---|---|---|
> | `ecf4937b` Bedroom M3 Pro | A000088 | streams; local control OK |
> | `7c89a5c1` Family Room | A000088 | streams; local control OK |
> | `444a7bf4` M3 Pro_180 | A000088 | **local control OK** (the table below says offline -- stale) |
> | `12b144cb` Winees Spotlight | A001064 | streams; the MSE packet-loss fix landed in `1.0.0b23` |
> | `338603b5` L2 F127 (driveway) | A001513 | streams, but its publisher **dies every ~3 min** (ffmpeg 255). RTP link measured clean at 0.05% |
> | `b5284fc7` L2_F8A3 (kitchen) | A001513 | **does not stream.** `first media never arrived (75s)`, `livePlayResp -50019`, ffmpeg exit 234 |
> | `dc3bf570` Deck | A000088 | **unavailable**, gone from the AiDot cloud since 2026-06-27, still ships 20 entities |
>
> ### Verdict of the re-run
>
> **Not ready for 1.0.0rc1.** Two gates fail independently:
>
> 1. `ROAD-TO-1.0.md` item 1 asks for **two weeks with no streaming-breaking
>    release**. `1.0.0b23` (2026-08-23) fixed live playback dying on a camera.
>    That restarts the clock. Six library releases landed in the four days to
>    2026-08-23. Earliest honest rc1 is **2026-09-06**, and only if nothing
>    structural lands first.
> 2. This audit's own coverage: every control on A001064 and A001513 is class B,
>    and the missing method step has now produced a defect twice.
>
> Three cameras need an explicit classification before rc1 -- the bar asks that
> the shape has settled, not that nothing is broken, so "accepted and documented"
> is a valid answer, but silence is not. See `## Camera classification` below.
>
> ---


Measured 2026-08-14 against the reference fleet, on library `1.0.0b10` and
integration `2.14.0`. Companion to [ROAD-TO-1.0.md](ROAD-TO-1.0.md), which sets
the release bar; this file answers a narrower question that has to be settled
before that bar's clock can start: **does every feature we ship actually do
what it claims?**

## Why an ack is not evidence

This firmware acknowledges writes it then ignores. Of fourteen attributes
probed on 2026-08-14, **eight acked and kept their own value**. A feature that
"works" in the sense that no exception was raised is not a feature that works.

The same day produced three cases of code that looked healthy and did nothing:
the DTLS stdout producer (opened a session, wrote zero bytes, forever, with no
error), `async_open_cloud_playback` (dead at its first handshake step), and the
TUTK live path (needs a `p2pId` no device returns). All three passed a green
test suite, because the tests called the helpers directly and never asked who
called them.

So every row below is classified by evidence of **effect**, not existence:

| Class | Meaning |
|---|---|
| **A** | Effect observed on hardware - write confirmed by read-back, or media decoded |
| **B** | Exercised end to end, effect not independently confirmed (ack only) |
| **C** | Unit-tested only, or no verified call path from a user action |
| **D** | No caller - never executed outside its own tests |
| **E** | Known broken |

Scope is deduped by **entity key x model family** rather than by entity. 119
entities across 7 cameras are about 23 distinct features; the three model
families (A000088 DTLS, A001064 PTZ/SDES, A001513 battery/SDES) are where real
behavioural differences live.

## Verdict

**Two real defects found, both now fixed.** One was user-facing: a switch that
toggled and changed nothing.

Two of the five items in the first pass of this audit did not survive scrutiny,
and both corrections are recorded below rather than quietly edited out - an
audit that only ever finds things is not being read carefully enough either.

Everything else verified: 16 features in class A on hardware, including every
control that had never been confirmed by read-back, on all three model
families. One control stays at class B on purpose - see the PTZ buttons row.

## Fleet used

| Device | Model | Family | State |
|---|---|---|---|
| `7c89a5c1` Family Room | A000088 | DTLS, mains | online, LAN-controllable |
| `ecf4937b` Bedroom M3 Pro | A000088 | DTLS, mains | online, LAN-controllable |
| `12b144cb` Winees Spotlight | A001064 | SDES, mains, PTZ | online |
| `338603b5`, `b5284fc7` L2 | A001513 | SDES, battery | online |
| `444a7bf4` M3 Pro_180 | A000088 | DTLS, mains | **offline at the cloud since 2026-08-11** |
| `dc3bf570` Deck | A000088 | DTLS, mains | **offline at the cloud since 2026-06-27** |

Home Assistant reports exactly those two cameras `unavailable` and no others.
That is the availability logic behaving correctly on real offline hardware, and
it is the first thing checked - two dark cameras would otherwise have been read
as a defect.

## Streaming and media

| Feature | Family | Class | Evidence |
|---|---|---|---|
| Live view, DTLS | A000088 | **A** | go2rtc http-listen serve in daily production use; CI live-validate green on the b9 and b10 shas |
| Live view, SDES | A001064, A001513 | **A** | same |
| Stdout producer (`aidot-go2rtc <id> -`) | A000088 | **A** | 1.47 MB MPEG-TS; longer run decoded h264 1280x720 + 48 kHz AAC. Was 0 bytes before the b9 fix |
| RTSP push (`rtsp://`) | A000088 | **A** | published into a live go2rtc, pulled back as `h264 1280x720` + `pcm_alaw` |
| RTSP push | A001064, A001513 | **A** | the long-standing SDES path, in production |
| Cloud recording list | A001064 | **A** | 10 clips returned for the last 7 days |
| Cloud clip playback URL | A001064 | **A** | `async_get_event_video_media` returned a signed HLS URL; fetch returned HTTP 200 |
| SD-card recording list | A000088 | **A** | shipped and stable since integration 2.13.1 |
| SD-card playback | A000088 | **n/a** | closed as not feasible; the media browser marks these items `can_play=False` rather than offering something that fails |
| Two-way audio | A001064, A001513 | **B, and cannot go higher unattended** | the ceiling for an automated test is "the library sent audio"; whether the speaker produced sound needs someone in the room, and the cameras are in occupied rooms so tests play silence. Deliberately left at B rather than dressed up - an ack is what this firmware gives for things it ignores |
| Live view through Home Assistant, **battery** | A001513 | **A** | `camera/stream` returns an HLS playlist, a live `rtsp` producer appears in go2rtc, and pulling it while HA holds the stream open gives `h264 1280x960` + `pcm_alaw`. Earlier project notes describing this path as broken are stale |

## Controls

Every row measured by the same method: read the attribute, write the other
value, read it back, restore, confirm the restore took. Run over the device's
local TCP channel from the LAN runner, against **two** A000088 units.

| Entity | Wire attribute | Family | Class | Result |
|---|---|---|---|---|
| `switch.*_osd_timestamp` | `OSDEnable` | A000088 | **A** | read-back confirmed |
| `switch.*_hdr` | `HDRStatus` | A000088 | **A** | read-back confirmed |
| `switch.*_auto_light` | `autoLightEnable` | A000088 | **A** | read-back confirmed |
| `switch.*_voice_prompts` | `voiceEnable` | A000088 | **A** | read-back confirmed |
| `switch.*_motion_detection` | `MotionDetection_Enable` | A000088 | **A** | read-back confirmed |
| `switch.*_microphone` | `micEnable` | A000088 | **A** | read-back confirmed on both units |
| `switch.*_status_led` | `LedOnOff` | A000088 | **A** | read-back confirmed |
| `number.*_motion_sensitivity` | `MotionDetection_Sen` | A000088 | **A** | 1 -> 2 confirmed |
| `number.*_speaker_volume` | `SoundLevel` | A000088 | **A** | 4 -> 2 confirmed |
| `select.*_night_vision` | `nightVisionMode` | A000088 | **A** | 1 -> 2 confirmed |
| `switch.*_ir_light` | `nightVisionIRLight` | A000088 | **E -> fixed** | acks and ignores on both units; entity removed - see E1 |
| `switch.*_auto_tracking` | `trackingMode` | A001064 | **A** | on -> off confirmed by entity read-back in 25 s, then restored |
| `button.*_ptz_*` (pan only) | AVIO PTZ 0x1001 | **A** | the camera pans. Mean frame difference across a 5 s pan: **25.2 at speed 4, 38.6 at speed 200**, against a **no-command control of 2.6** on the same session and timings. The camera advertises `ptzDirection [6,3]` - pan only, no tilt or zoom - and the button gate honours it. Two earlier readings called this inert; both were instrument failures (cached camera-proxy still; short repeated go2rtc pulls returning near-identical keyframes) |
| `siren.*` | `async_set_siren` | all | **C** | deliberately not fired - see "not tested" |
| Local (LAN) control | TCP:10000 | A000088 | **A** | 58 attributes read on both units, `eligible=True`, writes confirmed above |
| Floodlight | `LightOnOff` | A001064 | **A** | off -> on confirmed by entity read-back in 25 s, then restored to off |

## Control coverage: every property x every eligible model

The first pass of this document verified the full control set on the **A000088
only**; the other two families had two of eleven each. Completed 2026-08-15 via
the HA entity read-back path (drive the real service, wait one coordinator
poll, read back, restore, confirm the restore).

| Control | A000088 (DTLS mains) | A001064 (SDES PTZ) | A001513 (SDES battery) |
|---|---|---|---|
| `timestamp_overlay` (`OSDEnable`) | **A** x2 units | **A** | **A** x2 units |
| `hdr` (`HDRStatus`) | **A** x2 units | **A** | **A** x2 units (see note) |
| `voice_prompts` (`voiceEnable`) | **A** x2 units | **A** | **A** x2 units |
| `floodlight_automation` (`autoLightEnable`) | **A** x2 units | **A** | **A** x2 units |
| `motion_detection` | **A** | **A** | **A** |
| `microphone` (`micEnable`) | **A** x2 units | **A** | **A** |
| `status_led` (`LedOnOff`) | **A** | **A** | **A** |
| `auto_tracking` (`trackingMode`) | n/a | **A** | **A** |
| `night_vision` (`nightVisionMode`) | **A** | **A** | **A** |
| `motion_sensitivity` | **A** | **A** | **A** |
| `speaker_volume` (`SoundLevel`) | **A** | **A** | **A** |
| `ir_light` (`nightVisionIRLight`) | **E** - acks, ignores | not offered | not offered |

Every cell is a confirmed read-back, not an ack.

**One note worth keeping.** `hdr` on one A001513 read `TAKES=NO` on its first
attempt, then took on the sibling unit and on two immediate retests of the same
unit. So it is a **timing artifact on a sleeping battery camera**, not a defect
- the write lands but can miss a 90 s poll window. Recorded because a single
failing sample here would have looked exactly like the `ir_light` defect, and
the difference between them is only that one was retested on a second unit.

## Sensors and events

| Entity | Class | Evidence |
|---|---|---|
| `sensor.*_battery` | **A** | 39% and 45% on the two A001513 units - plausible, and they differ |
| `sensor.*_sd_card_status` | **A** | `present` / `empty` on the two A000088s. Reads `unknown` on A001064/A001513, which do not report the attribute at all |
| `sensor.*_wifi_rssi` | **A** | every camera reports `networkRssi` (-11 to -62 dBm, all seven). No entity appears by default because the description sets `entity_registry_enabled_default=False` - an opt-in diagnostic, not dead code. See "corrections" |
| `binary_sensor.*_occupancy` | **A** | tracks motion; states current across the fleet |
| `event.*_motion` | **A** | last fired 2026-08-14 16:46 UTC on `driveway` - real traffic, not a placeholder |
| `camera.*` availability | **A** | exactly the two cloud-offline cameras are `unavailable` |
| Diagnostics | **B** | redaction added and unit-tested; not re-downloaded here |

## Install and distribution

| Check | Class | Evidence |
|---|---|---|
| Published wheel installs | **A** | `1.0.0b10` installed from PyPI into a clean venv on arm64 Linux and ran the control sweep - the audit itself used the published artifact, not the working tree |
| Pre-release containment | **A** | PyPI `info.version` still reads `0.17.2`, so plain `pip` and HACS see the last stable |
| HACS snapshot integrity | **A** | published tree contains no `scripts/` or `UPSTREAM-SYNC.md`; branch protection restored after the force-push |

## Findings, and what happened to them

**E1 - `switch.*_ir_light` acked and did nothing (A000088). FIXED.**
`nightVisionIRLight` accepted the write, returned success, and kept its
previous value - on both A000088 units, so model behaviour rather than one bad
camera. A user could toggle this switch in Home Assistant and nothing happened,
with no error. The entity is removed, a test pins its absence, and the library
setter now documents the measurement (it is kept: the attribute is real and
another model may honour it, but `True` is not evidence of change).

**E2 - `async_get_camera_attributes` returned nothing. FIXED.** It appended
`-cmd` to the account's registered `mqttClientId`, and the broker binds the
credential to that exact string: the connect was refused outright with CONNACK
**rc=4, "Bad user name or password"** - no session, no subscription, nothing to
receive. It had never worked, on any camera, since it was written. Measured
with Home Assistant's entry disabled so nothing competed for the id, and A/B'd
in the same run: suffixed refused, exact accepted (rc=0).

It now uses the exact id and prefers the shared persistent connection
unconditionally - same identity, no second connect, so neither refused nor
evicting the holder. The one-off fallback declines while a stream is active
rather than displacing it.

**Verified on hardware:** an A001513 battery camera returns its pushed
attributes where it previously returned `None`. Mains cameras answer the
presence announce with nothing at all - which matches what the call is for,
since the wake-then-read sequence exists so a sleeping battery camera reports
battery, SD-card and occupancy. The deprecation added earlier the same day is
withdrawn: a method that works should not carry a warning saying it cannot.

Three readings of this were wrong before the cause was found, in order: "the
camera never pushes it", "the cause cannot be established", "the subscription
is broken". All three came from probes calling `_mqtt_session`, which returns
messages and drops the transport status behind a WARNING that every probe had
suppressed. Asking `_mqtt_session_with_status` answered it in one run. **When a
measurement says "nothing came back", check whether the transport reported why
before theorising about the far end** - and pair it with a request known to
answer, which is what finally settled the sibling case.

**E3 - `async_open_kvs_stream`** - the pre-rename alias, deprecated in 1.0.0b9,
removal at 1.0.0.

**D1 - `custom_components/aidot/sd_playback.py` had no importer. REMOVED.**
Three helpers reachable only from their own tests, shipped in every HACS
snapshot, left over from an SD-playback stage closed as not feasible. The SD
browser is unaffected.

**D3 - `async_open_live_stream` (TUTK)** - accepted with a stated reason; item
4 of ROAD-TO-1.0 already put this path out of scope.

## Two corrections to the first pass of this audit

Both were called findings and neither was one. They are kept here rather than
edited away, because an audit that never corrects itself is not being read.

**"D2 - the `wifi_rssi` sensor never materialises" was wrong.** No entity
appears because the description sets `entity_registry_enabled_default=False` -
it is an opt-in diagnostic, exactly as intended. And the data is there: all
seven cameras report `networkRssi`, from -11 to -62 dBm. The mistake was
inferring dead code from an absent entity without checking why it was absent.

**"C1 - the A001064's controls cannot be read back at all" was wrong.** It is
true that the PTZ model has no local control and that E2 is broken, but a third
read-back path was sitting in plain sight: the coordinator's device-properties
poll, which is what populates every entity. Driving Home Assistant's own
service and reading the entity back confirmed `speaker_volume` 2 -> 3 and
`auto_tracking` on -> off within 25 s, both then restored. So the PTZ family is
verifiable after all, by the path users actually exercise - and that method is
cheaper than the LAN sweep, needs no credentials beyond the HA token, and works
on every model.

## A stronger orphan sweep, and its limits

The sweep that found `async_open_cloud_playback` looked for functions
referenced *only by tests*. A second pass looking for **zero references
anywhere** (package, tests, and the reference integration) returns 22, but most
are false positives and the distinction matters for anyone re-running it:

- **Decorator-registered handlers** - `_on_track_rec`, `_on_ice_state`,
  `_on_conn_state` and friends are invoked by aiortc through `@pc.on(...)`,
  never by name.
- **Public API** - `load_credentials`, `save_credentials`, `async_set_ir_light`
  and similar exist for consumers, not for internal callers.

What survives both filters is a short list of genuinely unreferenced private
helpers: `_mqtt_listen` (protocol.py), `_compiled_in` (hwaccel.py) and
`_sctp_init` (sdes_open.py). None is on a live path and none is worth deleting
in the middle of a long session on shared streaming code - recorded here so the
next person can decide with fresh eyes.

## Deliberately not tested, and why

- **The 7 sirens.** Firing a camera alarm in an occupied house is not a test,
  it is an incident. Same reasoning that makes two-way audio tests play silence.
- **Battery-camera controls are no longer in this list.** Swept 2026-08-15 via
  the HA entity read-back path: `motion_sensitivity` 1 -> 2 and `status_led`
  on -> off both took and restored on an A001513. The model family is covered.
- **Reboot / factory-reset / SD-format.** None exist. All 11 buttons are PTZ
  movement, which is why button testing carried no destructive risk.
- **The 7 sirens** stay untested for the reason above.

- **The uncancellable-hang fix is now soaked: 0 hangs in 10 consecutive opens**
  (~12 s each, one unrelated `-50002` busy refusal from Home Assistant holding
  the camera). Against two isolated pre-fix runs that both hung on the second
  open, and a unit test that hangs outright on the old code, that is as close to
  settled as an intermittent failure gets.

## What the countdown depends on

ROAD-TO-1.0's remaining bar is two weeks with no streaming-breaking release.
Nothing here is streaming-breaking: E1 was a settings switch, E2 is an uncalled
API, D1 was dead code. So none of them reset that clock - but shipping the
fixes starts a release, and that does.

So the clock starts at **the release that carries these fixes**, not at the
audit. What it now rests on is a surface where every user-facing feature has
been exercised for effect on at least one model of each family, the two defects
that turned up are closed, and the method for checking the next one is written
down: drive Home Assistant's own service, wait one coordinator poll, read the
entity back. Anything that acks and does not change is not shipped.

## How to re-run this

The cheap path, no credentials beyond the HA token, works on every model:

    # read -> write via the real service -> wait one poll -> read back -> restore
    curl -H "Authorization: Bearer $TOKEN" $HA/api/states/<entity>
    curl -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
         -d '{"entity_id":"<entity>"}' $HA/api/services/switch/turn_off
    sleep 30 && curl -H "Authorization: Bearer $TOKEN" $HA/api/states/<entity>

The LAN sweep is stronger where it applies (it reads all 58 attributes and
needs no HA at all) but only the mains A000088s advertise local control, and it
must run from a host on the LAN - macOS blocks the UDP discovery and the TCP
control socket for a non-permitted binary, so it runs from the LAN runner.

For library-level paths with no entity, reproduce the destination synthetically
before spending hardware cycles: a `testsrc` + `sine` MPEG-TS through the exact
argv the code composes caught the RTSP-push AAC failure in seconds, after three
hardware attempts had failed short of it for unrelated reasons.

---

## Camera classification (added 2026-08-23, for the rc1 decision)

`ROAD-TO-1.0.md` asserts that the shape has settled, not that nothing is broken,
and two of its six items closed as "documented as accepted". These three
cameras were sitting in neither state. Classified here so rc1 is not blocked on
silence.

### `b5284fc7` L2_F8A3 / kitchen -- MUST FIX or MUST EXCLUDE

Measured 2026-08-23: does not stream at all.

    SDES first media never arrived (75s). nominated=3.2
    camera not ready (waking, livePlayResp -50019) and sent no media
    SDES bridge: ffmpeg exited with code 234

The camera answers signalling and then sends nothing. This is a whole camera
that never produces video, not a degradation, and it is the one state the bar
explicitly forbids: unknown. It is now known and it is bad.

Shipping 1.0.0 with a camera in the reference fleet that cannot stream needs
either a fix or a written statement of which models/conditions are unsupported.
Recommendation: **must resolve before rc1** -- at minimum determine whether this
is the camera, the battery/wake path, or its radio, so the statement can be
accurate.

### `338603b5` L2 F127 / driveway -- ACCEPTED AND DOCUMENTED (proposed)

Streams correctly, but the publisher dies roughly every three minutes with
ffmpeg exit 255. Its RTP link measured clean the same day (0.05% loss, zero
mid-frame), so this is not the packet-loss class fixed in `1.0.0b23`.

A viewer sees live video that stops after a few minutes and recovers on the next
open. Unpleasant, bounded, understood in shape if not in cause. Recommendation:
**accepted and documented for 1.0.0**, with the symptom named in the
integration's troubleshooting so nobody reports it as new.

### `dc3bf570` Deck -- ACCEPTED AND DOCUMENTED (proposed)

Gone from the AiDot cloud since 2026-06-27 and `unavailable` in Home Assistant,
yet it still contributes 20 entities.

This is correct behaviour for a device the cloud still lists but no longer
serves -- the availability logic is doing its job -- and the 2026-08-14 audit
already checked that exactly the offline cameras report unavailable and no
others. Recommendation: **accepted and documented**, with a line telling users
that a camera removed at the vendor stays in the registry until they delete it.

### What "before rc1" therefore means

1. Resolve or write up kitchen.
2. Land the two acceptance notes in the integration's user docs.
3. Close the class-B cells for A001064 and A001513, or state that they stay
   class B and why.
4. Two weeks with no streaming-breaking release, from 2026-08-23.
